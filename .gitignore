# Dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Environment & secrets
.env
.env.local
.env.*.local
!.env.example

# Build outputs
.next/
out/
build/
dist/
*.tsbuildinfo

# IDE / tooling
.cursor/
.vercel
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Prisma generated
**/src/generated/prisma/

# OS
Thumbs.db

# Tooling
.superpowers/

# Vendored third-party clones (have their own .git, not part of this repo)
space-cadet-pinball/

# Plat PDF download cache (indexes in data/plats/*.json are kept)
data/plats/cache/

# Scraper local caches (surplus snapshots may contain Pro-only data)
scraper/.cache/

# Apify raw scrape dumps (derived companies.json is kept)
data/apify-raw.json
data/.ingest-cache.json
data/.ingest-progress.json

# Firecrawl self-host clone (own repo)
firecrawl/

data/.enrich-cities-cache.json
data/.enrich-reviews-cache.json

data/.firecrawl-auth.json
data/.firecrawl-auth-url.txt

# Temporary DBPR license extract downloads (regenerate via scripts/build_fl_plumbing_hvac_spreadsheet.py)
tmp-fl-contractors/
